# git-cliff ~ configuration file
# https://git-cliff.org/docs/configuration

[changelog]
header = """
# Changelog\n
All notable changes to this project will be documented in this file.\n
"""
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}**{{ commit.scope }}:** {% endif %}\
            {% if commit.breaking %}[**BREAKING**] {% endif %}\
            {{ commit.message | upper_first }}\
            {% if commit.links %} \
                {% for link in commit.links %}([{{ link.text }}]({{ link.href }})){% endfor %}\
            {% endif %}\
    {% endfor %}
{% endfor %}\n
"""
footer = """
<!-- generated by git-cliff -->
"""
trim = true
render_always = true
postprocessors = [
    # Uncomment to auto-link issues:
    # { pattern = '<REPO>', replace = "https://github.com/nhat092005/build-os" },
]

[git]
conventional_commits = true
filter_unconventional = true
require_conventional = false
split_commits = false

commit_preprocessors = [
    # Auto-link GitHub issue numbers like (#123)
    # { pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](<REPO>/issues/${2}))"},
]

protect_breaking_commits = true

commit_parsers = [
    { message = "^feat",                group = "<!-- 0 -->Features"            },
    { message = "^fix",                 group = "<!-- 1 -->Bug Fixes"            },
    { message = "^refactor",            group = "<!-- 2 -->Refactor"             },
    { message = "^docs",                group = "<!-- 3 -->Documentation"        },
    { message = "^perf",                group = "<!-- 4 -->Performance"           },
    { message = "^style",               group = "<!-- 5 -->Styling"              },
    { message = "^test",                group = "<!-- 6 -->Testing"              },
    { message = "^revert",              group = "<!-- 7 -->Revert"               },
    { body    = ".*security",           group = "<!-- 8 -->Security"             },
    { message = "^chore\\(release\\)",  skip  = true                               },
    { message = "^chore\\(deps",        skip  = true                               },
    { message = "^chore\\(pr\\)",       skip  = true                               },
    { message = "^chore\\(pull\\)",     skip  = true                               },
    { message = "^chore|^ci",           group = "<!-- 9 -->Miscellaneous"       },
    { message = ".*",                   group = "<!-- 10 -->Other"              },
]

filter_commits = false
fail_on_unmatched_commit = false
link_parsers = [
    # Parse PR links like #123
    { pattern = "#(\\d+)", href = "https://github.com/nhat092005/build-os/issues/$1", text = "#$1" },
]

use_branch_tags = false
topo_order = false
topo_order_commits = true
sort_commits = "oldest"
recurse_submodules = false